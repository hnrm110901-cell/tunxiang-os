# Example Plugin

This is a scaffolded plugin placeholder.

## Next steps

1. Update `plugin.json` metadata.
2. Implement plugin logic in `src/index.js`.
3. Add assets to the `assets/` folder if needed.
