pipeline:
  name: ci-tx-menu
  identifier: ci_tx_menu
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-menu 菜品菜单服务 CI 流水线。PR 或 main 分支推送时自动触发，执行 Lint/安全扫描/测试/镜像构建。含菜品三级发布逻辑。"
  tags:
    service: tx-menu
    type: ci
  properties:
    ci:
      codebase:
        connectorRef: github_connector
        repoName: tunxiang-os
        build: <+input>
  stages:
    - stage:
        name: CI
        identifier: CI
        type: CI
        variables:
          - name: servicePath
            type: String
            value: services/tx-menu
          - name: imageRepo
            type: String
            value: tunxiang/tx-menu
          - name: dockerfile
            type: String
            value: services/tx-menu/Dockerfile
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: k8s_connector
              namespace: harness-ci
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  name: CI_Steps
                  identifier: CI_Steps
                  template:
                    templateRef: tpl_ci_python_service
                    versionLabel: v1
                    templateInputs:
                      type: StepGroup
