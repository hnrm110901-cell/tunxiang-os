pipeline:
  name: ci-tx-growth
  identifier: ci_tx_growth
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-growth 服务 CI 流水线。PR 或 main 分支推送时自动触发，执行 Lint/安全扫描/测试/镜像构建。"
  tags:
    service: tx-growth
    type: ci
  properties:
    ci:
      codebase:
        connectorRef: github_connector
        repoName: tunxiang-os
        build: <+input>
  stages:
    - stage:
        name: CI
        identifier: CI
        type: CI
        variables:
          - name: servicePath
            type: String
            value: services/tx-growth
          - name: imageRepo
            type: String
            value: tunxiang/tx-growth
          - name: dockerfile
            type: String
            value: services/tx-growth/Dockerfile
        spec:
          cloneCodebase: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: k8s_connector
              namespace: harness-ci
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  name: CI_Steps
                  identifier: CI_Steps
                  template:
                    templateRef: tpl_ci_python_service
                    versionLabel: v1
                    templateInputs:
                      type: StepGroup
