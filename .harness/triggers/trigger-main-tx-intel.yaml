trigger:
  name: trigger-main-tx-intel
  identifier: trigger_main_tx_intel
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-intel 服务 main 分支推送触发器。push to main 时触发 cd_tx_intel 流水线（部署到 staging）。"
  tags:
    service: tx-intel
    event: push-main
  enabled: true
  pipelineIdentifier: cd_tx_intel
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: Push
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: false
          payloadConditions:
            - key: targetBranch
              operator: Equals
              value: main
          headerConditions: []
          jexlCondition: ""
          actions: []
  inputYaml: |
    pipeline:
      identifier: cd_tx_intel
      properties:
        ci:
          codebase:
            build:
              type: branch
              spec:
                branch: main
