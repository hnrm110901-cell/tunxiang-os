trigger:
  name: trigger-pr-web-crew
  identifier: trigger_pr_web_crew
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "web-crew 服务员端前端 PR 触发器。PR opened/synchronize 时触发 ci_web_crew 流水线。"
  tags:
    service: web-crew
    event: pr
  enabled: true
  pipelineIdentifier: ci_web_crew
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: PullRequest
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: true
          payloadConditions:
            - key: targetBranch
              operator: In
              value: main,develop
          headerConditions: []
          jexlCondition: ""
          actions:
            - opened
            - synchronize
            - reopened
  inputYaml: |
    pipeline:
      identifier: ci_web_crew
      properties:
        ci:
          codebase:
            build:
              type: PR
              spec:
                number: <+trigger.prNumber>
