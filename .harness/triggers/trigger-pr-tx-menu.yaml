trigger:
  name: trigger-pr-tx-menu
  identifier: trigger_pr_tx_menu
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-menu 服务 PR 触发器。PR opened/synchronize 时触发 ci_tx_menu 流水线。"
  tags:
    service: tx-menu
    event: pr
  enabled: true
  pipelineIdentifier: ci_tx_menu
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: PullRequest
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: true
          payloadConditions:
            - key: targetBranch
              operator: In
              value: main,develop
          headerConditions: []
          jexlCondition: ""
          actions:
            - opened
            - synchronize
            - reopened
  inputYaml: |
    pipeline:
      identifier: ci_tx_menu
      properties:
        ci:
          codebase:
            build:
              type: PR
              spec:
                number: <+trigger.prNumber>
