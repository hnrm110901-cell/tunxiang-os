trigger:
  name: trigger-main-tx-menu
  identifier: trigger_main_tx_menu
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-menu 服务 main 分支推送触发器。push to main 时触发 cd_tx_menu 流水线（部署到 staging）。"
  tags:
    service: tx-menu
    event: push-main
  enabled: true
  pipelineIdentifier: cd_tx_menu
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: Push
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: false
          payloadConditions:
            - key: targetBranch
              operator: Equals
              value: main
          headerConditions: []
          jexlCondition: ""
          actions: []
  inputYaml: |
    pipeline:
      identifier: cd_tx_menu
      properties:
        ci:
          codebase:
            build:
              type: branch
              spec:
                branch: main
