trigger:
  name: trigger-main-tx-brain
  identifier: trigger_main_tx_brain
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-brain 服务 main 分支推送触发器。push to main 时触发 cd_tx_brain 流水线（部署到 staging，使用 Mock LLM）。"
  tags:
    service: tx-brain
    event: push-main
  enabled: true
  pipelineIdentifier: cd_tx_brain
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: Push
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: false
          payloadConditions:
            - key: targetBranch
              operator: Equals
              value: main
          headerConditions: []
          jexlCondition: ""
          actions: []
  inputYaml: |
    pipeline:
      identifier: cd_tx_brain
      properties:
        ci:
          codebase:
            build:
              type: branch
              spec:
                branch: main
