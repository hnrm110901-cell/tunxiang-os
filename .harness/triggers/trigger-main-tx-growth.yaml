trigger:
  name: trigger-main-tx-growth
  identifier: trigger_main_tx_growth
  projectIdentifier: TunXiangOS
  orgIdentifier: default
  description: "tx-growth 服务 main 分支推送触发器。push to main 时触发 cd_tx_growth 流水线（部署到 dev/test/uat）。"
  tags:
    service: tx-growth
    event: push-main
  enabled: true
  pipelineIdentifier: cd_tx_growth
  source:
    type: Webhook
    spec:
      type: Github
      spec:
        type: Push
        spec:
          connectorRef: github_connector
          repoName: tunxiang-os
          autoAbortPreviousExecutions: false
          payloadConditions:
            - key: targetBranch
              operator: Equals
              value: main
          headerConditions: []
          jexlCondition: ""
          actions: []
  inputYaml: |
    pipeline:
      identifier: cd_tx_growth
      properties:
        ci:
          codebase:
            build:
              type: branch
              spec:
                branch: main
