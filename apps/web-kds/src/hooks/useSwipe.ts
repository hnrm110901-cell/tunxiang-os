/**
 * useSwipe — 兼容层
 *
 * 已迁移至 @tunxiang/design-system 共享包。
 * 此文件保留 re-export 以兼容现有 import。
 */
export { useSwipe } from '@tx-ds/hooks/useSwipe';
export type { UseSwipeOptions, UseSwipeReturn } from '@tx-ds/hooks/useSwipe';
