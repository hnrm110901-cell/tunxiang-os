import { Placeholder } from '../_Placeholder'

export default function EdgePage() {
  return (
    <Placeholder
      no="11"
      title="边缘门店"
      description="Mac mini M4 节点纳管：sync-engine 状态、Core ML 健康、Tailscale 在线。"
      todos={['门店地图（Tailscale 连通性）', 'sync-engine 增量同步监控', 'launchd 服务远程重启']}
    />
  )
}
