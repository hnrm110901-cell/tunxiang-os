import { Placeholder } from '../_Placeholder'

export default function ConfigPage() {
  return (
    <Placeholder
      no="09"
      title="配置中心"
      description="集中式配置 + 多环境 diff + 热更新订阅。"
      todos={['配置项 CRUD（Monaco Editor）', '变更审计 + 回滚', '租户级覆盖（RLS）']}
    />
  )
}
