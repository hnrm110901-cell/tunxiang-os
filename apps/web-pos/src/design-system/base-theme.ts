/**
 * @deprecated 本文件已迁移到 `@tx/touch/styles/base-theme`（2026-05-07，S1-01 / #244）。
 * 请在新代码中使用：
 *   import { TX, BTN, INPUT, TAG, LAYOUT, TXT } from '@tx/touch';
 *
 * 本 shim 仅为兼容潜在动态引用保留，下一次清理（S1-08 / #251）将移除。
 */

export { TX, TXT, BTN, INPUT, TAG, LAYOUT } from '@tx/touch';
