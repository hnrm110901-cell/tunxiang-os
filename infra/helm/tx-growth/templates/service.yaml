apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-growth.fullname" . }}
  labels:
    {{- include "tx-growth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-growth.selectorLabels" . | nindent 4 }}
