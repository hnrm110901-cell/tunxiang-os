apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-intel.fullname" . }}
  labels:
    {{- include "tx-intel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-intel.selectorLabels" . | nindent 4 }}
