apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-org.fullname" . }}
  labels:
    {{- include "tx-org.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-org.selectorLabels" . | nindent 4 }}
