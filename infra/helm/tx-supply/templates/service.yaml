apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-supply.fullname" . }}
  labels:
    {{- include "tx-supply.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-supply.selectorLabels" . | nindent 4 }}
