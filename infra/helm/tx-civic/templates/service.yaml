apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-civic.fullname" . }}
  labels:
    {{- include "tx-civic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-civic.selectorLabels" . | nindent 4 }}
