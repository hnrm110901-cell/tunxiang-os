apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-predict.fullname" . }}
  labels:
    {{- include "tx-predict.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-predict.selectorLabels" . | nindent 4 }}
