apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-brain.fullname" . }}
  labels:
    {{- include "tx-brain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-brain.selectorLabels" . | nindent 4 }}
