apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-expense.fullname" . }}
  labels:
    {{- include "tx-expense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-expense.selectorLabels" . | nindent 4 }}
