apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-ops.fullname" . }}
  labels:
    {{- include "tx-ops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-ops.selectorLabels" . | nindent 4 }}
