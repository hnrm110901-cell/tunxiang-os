apiVersion: v1
kind: Service
metadata:
  name: {{ include "tx-devforge.fullname" . }}
  labels:
    {{- include "tx-devforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tx-devforge.selectorLabels" . | nindent 4 }}
